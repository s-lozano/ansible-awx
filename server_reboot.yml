---

- name: Restart services
  hosts: webapache
  gather_facts: false

  tasks:
    - name: Apache
      service:
        name: "{{ item }}" 
        state: restart #restarted
      with_items: 
        - apache2
...
