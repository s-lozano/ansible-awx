---
- name: Restart services
  gather_facts: false
  hosts: 10.0.2.15
  vars:
    ansible_python_interpreter: /usr/bin/python3
  become: yes
  
  tasks:
    - name: Restart apache2
      service:
        name:  apache2
        state: stopped
...
