---

- name: Restart services
  hosts: 10.0.2.2
  gather_facts: false

  tasks:
    - name: Apache
      service:
        name: "{{ item }}" 
        state: restart #restarted
      with_items: 
        - apache2
...
