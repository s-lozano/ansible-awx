---

- name: Restart services
  hosts: 10.0.2.15
  gather_facts: false

  tasks:
    - name: Restart MySQL and Apache
      service:
        name: "{{ item }}" 
        state: restarted
      with_items: 
        - mysql
        - apache2
...
