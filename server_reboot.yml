---

- name: Restart services
  hosts: lab-awx.ddns.net
  gather_facts: false

  tasks:
    - name: Restart MySQL and Apache
      service:
        name: "{{ item }}" 
        state: restarted
      with_items: 
        - ssh
...
