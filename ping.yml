---

- name: Basic playbook run locally 
  gather_facts: true 
  hosts: 10.0.2.20 
  tasks: 
    - name: Doing a ping
      ping:

    - name: Show info
      debug:
        msg: "Machine name: {{ ansible_hostname }}"
...
